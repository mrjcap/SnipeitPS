# Handoff: SnipeitPS v1.14.0 Audit — 2026-04-01

## What was done

8-round multi-agent code review (R1-R8) covering 114 public functions + 10 private helpers. 111 agents across 2 final rounds (R7: 175 agents, R8: 60 agents). Total: 39 commits.

### Categories of fixes
- **Security**: API key redaction in debug output, TLS 1.2 enforcement, URI scheme validation, regex injection fix
- **Critical bugs**: Shared `$Values` hashtable corruption on multi-ID image uploads, malformed PS5 base64 data URI, POST-instead-of-GET on `Get-SnipeitFieldsetField`, `$requestable` bool default silently filtering all searches
- **Data integrity**: `$null` pipeline leak on pagination, `$false` auth failure leaking into pipeline, `$_` shadowing in catch blocks, StreamReader not disposed on PS5
- **Parameter validation**: `[Nullable[bool]]` for optional bools, `[ValidateRange]` on FK IDs, `[ValidateSet]` on maintenance types and status, `[SecureString]` for passwords, `$purchase_cost` standardized to `[string]`
- **Code quality**: ConfirmImpact High on all Remove-*, Medium on all Set-*, meaningful ShouldProcess targets across 74 functions, legacy param handling moved to begin{}, dead code removal
- **Documentation**: 72 doc/code param mismatches fixed, README badges updated, missing `$sort` parameter documented

### Version bump
- Module version: 1.14.0
- AppVeyor version: 1.14.0.{build}
- CHANGELOG updated with full details

### Test results
- 699 Pester v5 tests, all passing
- 162 files changed across the audit

## Current state

- 39 commits ahead of `origin/master` — not yet pushed
- 2 untracked files: `Compare-ParamTypes.ps1` (utility script), `docs/summaries/summary.md` (60-agent review output)
- All known issues from R1-R8 reviews resolved
- No open items

## What needs to be done next

1. Push to origin when ready
2. Publish v1.14.0 to PowerShell Gallery
3. Consider opening GitHub issues for items deferred (none currently)

## Blockers / Decisions

- None pending
